/**
 * Curated re-export of the lucide-react icons used across the app.
 * Callers may import from here for a stable, discoverable set, or import
 * directly from "lucide-react" for anything not listed.
 *
 * Lucide icons are flat, geometric, 1.5px stroke, currentColor — they inherit
 * text color and size from the surrounding element.
 */
export {
  // navigation / chrome
  Menu,
  X,
  ChevronDown,
  ChevronRight,
  ChevronLeft,
  ChevronUp,
  ArrowLeft,
  ArrowRight,
  ExternalLink,
  Search,
  Settings,
  LogOut,
  LogIn,
  User,
  Users,
  Store,
  LayoutDashboard,
  // actions
  Plus,
  Minus,
  Trash2,
  Pencil,
  Check,
  Filter,
  Upload,
  Download,
  RefreshCw,
  MoreHorizontal,
  // status / feedback
  Loader2,
  CircleAlert,
  CircleCheck,
  TriangleAlert,
  Info,
  Inbox,
  // domain
  Heart,
  Star,
  ShoppingCart,
  Package,
  FileText,
  Tag,
  CreditCard,
  type LucideIcon,
} from 'lucide-react'
